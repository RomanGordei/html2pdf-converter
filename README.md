# HTML → PDF Конвертер ⚡

Простое веб-приложение для конвертации HTML-файлов в PDF.

![Python](https://img.shields.io/badge/Python-3.11-blue)
![Flask](https://img.shields.io/badge/Flask-3.0-green)
![Docker](https://img.shields.io/badge/Docker-ready-blue)

## Возможности

- 📤 Загрузка файлов перетаскиванием (drag & drop) или выбором
- 📑 Пакетная конвертация нескольких файлов
- 📥 Скачивание по одному или архивом (.zip)
- 🎨 Современный тёмный интерфейс на русском языке
- 🐳 Готов к запуску в Docker

## Быстрый старт

### Вариант 1: Docker (рекомендуется)

```bash
# Клонируем репозиторий
git clone https://github.com/your-username/html2pdf-converter.git
cd html2pdf-converter

# Запускаем
docker-compose up -d

# Готово! Открываем http://localhost:5000
```

### Вариант 2: Локальный запуск

**Требования:**
- Python 3.9+
- Системные библиотеки для WeasyPrint

```bash
# Ubuntu/Debian
sudo apt install libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0

# macOS
brew install pango gdk-pixbuf libffi

# Клонируем и устанавливаем
git clone https://github.com/your-username/html2pdf-converter.git
cd html2pdf-converter

python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

pip install -r requirements.txt

# Запуск
python run.py
```

Откройте в браузере: **http://localhost:5000**

## Структура проекта

```
html2pdf-converter/
├── app/
│   ├── __init__.py      # Инициализация Flask
│   ├── routes.py        # API и маршруты
│   └── templates/
│       └── index.html   # Веб-интерфейс
├── static/
│   ├── css/
│   │   └── style.css    # Стили
│   └── js/
│       └── main.js      # Логика интерфейса
├── uploads/             # Загруженные HTML (временно)
├── outputs/             # Готовые PDF (временно)
├── run.py               # Точка входа
├── requirements.txt     # Python зависимости
├── Dockerfile          
├── docker-compose.yml  
└── README.md
```

## API

| Метод | Путь | Описание |
|-------|------|----------|
| `GET` | `/` | Веб-интерфейс |
| `POST` | `/convert` | Конвертация файлов (multipart/form-data) |
| `GET` | `/download/<session_id>/<filename>` | Скачать один PDF |
| `GET` | `/download-all/<session_id>` | Скачать все PDF архивом |
| `POST` | `/cleanup/<session_id>` | Очистить временные файлы |

## Конфигурация

Переменные окружения (опционально):

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `FLASK_ENV` | `production` | Режим работы |
| `MAX_CONTENT_LENGTH` | `16MB` | Максимальный размер файла |

## Технологии

- **Backend:** Flask, WeasyPrint, Gunicorn
- **Frontend:** Vanilla JS, CSS3
- **Контейнеризация:** Docker

## Лицензия

MIT License — используйте как хотите 🙂

---

*Сделано с ❤️ для удобной работы с документами*
